while (<>) {
$count++;
if (/Fichiers\/([^:]+)/) {
   $invalid=$1;
#   print "ow! ";
   $sick{$invalid}++;
   $invalids++ unless($sick{$invalid});
   }
else {
     $well++;
#     print;
     }
}

print "\nINVALID FILES...\n";
    for $f (sort keys %sick) {
    $errs=$sick{$f};
print "($errs) $f \n";
    }
        print $invalids, " of ", $count, " had 1 or more errors\n";
